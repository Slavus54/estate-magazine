---
import Layout from '../layouts/Layout.astro'
import PropertiesComponent from '../components/Properties'
import {TOWNS_API_URL} from '../env/env'

const response = await fetch(TOWNS_API_URL)
const towns = await response.json() 

---

<Layout folderTitle='Properties'>
    <PropertiesComponent towns={towns} client:load />
</Layout>